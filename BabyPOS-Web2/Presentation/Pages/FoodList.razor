@page "/food-list/{ShopId:int}"
@using BabyPOS_Web2.Application.DTOs
@using BabyPOS_Web2.Domain.Models
@inject BabyPOS_Web2.Infrastructure.Services.IApiService ApiService
@inject IJSRuntime JS
@inject NavigationManager NavigationManager

<PageTitle>Food List</PageTitle>

<div class="container py-3">
    <h2>@ShopDetail</h2>
    <div class="d-flex justify-content-between align-items-center mb-3">
        <button class="btn btn-outline-secondary" @onclick="GoBack">&lt; Back</button>
        <div class="d-flex gap-2">
            <button class="btn btn-warning" @onclick="GoToOrders">
                <i class="bi bi-list-check"></i> จัดการ Order
            </button>
            <button class="btn btn-info" @onclick="ShowTableManagementModal">
                <i class="bi bi-table"></i> จัดการโต๊ะ
            </button>
            <button class="btn btn-success" @onclick="ShowAddFoodModal">+ เพิ่มรายการอาหาร</button>
        </div>
    </div>

    <!-- Category Tabs -->
    <ul class="nav nav-tabs mb-3">
        @foreach (var cat in CategoryTabs)
        {
            <li class="nav-item">
                <button type="button" class="nav-link @(SelectedCategory == cat.Value ? "active" : "")" @onclick="() => SelectCategory(cat.Value)">@cat.Label</button>
            </li>
        }
    </ul>

    @if (isLoading)
    {
        <div class="text-center py-5">
            <div class="spinner-border" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
            <p class="mt-2">กำลังโหลดเมนูอาหาร...</p>
        </div>
    }
    else if (!FilteredMenuItems.Any())
    {
        <div class="text-center text-muted py-5">ไม่พบข้อมูลเมนูอาหาร</div>
    }
    else
    {
        <div class="row g-3">
            @foreach (var chunk in FilteredMenuItems.Chunk(3))
            {
                @foreach (var item in chunk)
                {
                    <div class="col-12 col-md-4 d-flex">
                        <div class="card flex-fill">
                            <div class="card-body">
                                @if (!string.IsNullOrEmpty(item.ImagePath))
                                {
                                    <img src="@item.ImagePath" alt="@item.Name" style="width:100%;max-width:120px;max-height:120px;object-fit:cover;display:block;margin:auto;" />
                                }
                                else
                                {
                                    <img src="/img/food/default.png" alt="no image" style="width:100%;max-width:120px;max-height:120px;object-fit:cover;display:block;margin:auto;opacity:0.5;" />
                                }
                                <h5 class="card-title mt-2">@item.Name</h5>
                                <div class="fw-bold">฿@item.Price</div>
                                <div class="text-muted">@GetCategoryText(item.Category)</div>
                                <div class="mt-2">
                                    <button class="btn btn-sm btn-outline-primary me-1" @onclick="() => ShowEditFoodModal(item)">แก้ไข</button>
                                    <button class="btn btn-sm btn-outline-danger" @onclick="() => DeleteFoodAsync(item.Id)">ลบ</button>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            }
        </div>
    }

    @if (!string.IsNullOrEmpty(errorMessage))
    {
        <div class="alert alert-danger mt-3">
            <strong>Error:</strong> @errorMessage
        </div>
    }

    @if (!string.IsNullOrEmpty(successMessage))
    {
        <div class="alert alert-success mt-3">
            <strong>Success:</strong> @successMessage
        </div>
    }
</div>

<!-- Food Modal for Add/Edit -->
@if (showFoodModal)
{
    <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">@(editFoodId == 0 ? "เพิ่มเมนูใหม่" : "แก้ไขเมนู")</h5>
                    <button type="button" class="btn-close" @onclick="HideFoodModal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">ชื่อเมนู</label>
                        <input type="text" class="form-control" @bind="newFoodName" placeholder="ชื่อเมนู" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">ราคา (บาท)</label>
                        <input type="number" class="form-control" @bind="newFoodPrice" min="0" step="0.01" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">หมวดหมู่</label>
                        <select class="form-select" @bind="newFoodCategory">
                            <option value="@FoodCategory.MainDish">อาหาร</option>
                            <option value="@FoodCategory.Dessert">ของหวาน</option>
                            <option value="@FoodCategory.Drink">เครื่องดื่ม</option>
                            <option value="@FoodCategory.Other">อื่นๆ</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">รูปภาพ</label>
                        <input type="text" class="form-control" @bind="newFoodImagePath" placeholder="URL รูปภาพ" />
                        @if (!string.IsNullOrEmpty(newFoodImagePath))
                        {
                            <img src="@newFoodImagePath" alt="Preview" class="mt-2" style="max-width: 150px; height: auto;" />
                        }
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="HideFoodModal">ยกเลิก</button>
                    <button type="button" class="btn btn-primary" @onclick="SaveFoodAsync" disabled="@isSaving">
                        @if (isSaving)
                        {
                            <span class="spinner-border spinner-border-sm me-2" role="status"></span>
                        }
                        @(editFoodId == 0 ? "เพิ่ม" : "อัปเดต")
                    </button>
                </div>
            </div>
        </div>
    </div>
}

<!-- Table Management Modal -->
@if (showTableModal)
{
    <div class="modal fade show d-block" tabindex="-1" style="background-color: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">
                        <i class="bi bi-table"></i> จัดการโต๊ะ
                    </h5>
                    <button type="button" class="btn-close" @onclick="HideTableModal"></button>
                </div>
                <div class="modal-body">
                    @if (!string.IsNullOrEmpty(tableErrorMessage))
                    {
                        <div class="alert alert-danger">
                            @tableErrorMessage
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(tableSuccessMessage))
                    {
                        <div class="alert alert-success">
                            @tableSuccessMessage
                        </div>
                    }

                    <!-- Add New Table Form -->
                    <div class="card mb-3">
                        <div class="card-header">
                            <h6 class="mb-0">เพิ่มโต๊ะใหม่</h6>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-8">
                                    <div class="mb-3">
                                        <label class="form-label">ชื่อโต๊ะ</label>
                                        <input type="text" class="form-control" @bind="newTableName" placeholder="ชื่อโต๊ะ" />
                                    </div>
                                </div>
                                <div class="col-md-4 d-flex align-items-end">
                                    <button class="btn btn-success w-100" @onclick="AddTableAsync" disabled="@isAddingTable">
                                        @if (isAddingTable)
                                        {
                                            <span class="spinner-border spinner-border-sm me-2" role="status"></span>
                                        }
                                        <i class="bi bi-plus"></i> เพิ่มโต๊ะ
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Tables List -->
                    <div class="card">
                        <div class="card-header">
                            <h6 class="mb-0">รายการโต๊ะ</h6>
                        </div>
                        <div class="card-body">
                            @if (isLoadingTables)
                            {
                                <div class="text-center py-3">
                                    <div class="spinner-border" role="status"></div>
                                    <div>กำลังโหลดโต๊ะ...</div>
                                </div>
                            }
                            else if (tables.Count == 0)
                            {
                                <div class="text-center py-3 text-muted">
                                    <i class="bi bi-table fs-2"></i>
                                    <div>ยังไม่มีโต๊ะ</div>
                                </div>
                            }
                            else
                            {
                                <div class="row">
                                    @foreach (var table in tables)
                                    {
                                        <div class="col-md-6 mb-2">
                                            <div class="card">
                                                <div class="card-body py-2">
                                                    <div class="d-flex justify-content-between align-items-center">
                                                        <div>
                                                            <h6 class="mb-0">@table.Name</h6>
                                                            <small class="text-muted">ID: @table.Id</small>
                                                        </div>
                                                        <button class="btn btn-sm btn-outline-danger" @onclick="() => DeleteTableAsync(table.Id)">
                                                            <i class="bi bi-trash"></i>
                                                        </button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    }
                                </div>
                            }
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" @onclick="HideTableModal">ปิด</button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public int ShopId { get; set; }
    
    private record CategoryTab(string Label, FoodCategory? Value);
    List<CategoryTab> CategoryTabs = new()
    {
        new("ทั้งหมด", null),
        new("อาหาร", FoodCategory.MainDish),
        new("ของหวาน", FoodCategory.Dessert),
        new("เครื่องดื่ม", FoodCategory.Drink),
        new("อื่นๆ", FoodCategory.Other)
    };
    
    FoodCategory? SelectedCategory = null;
    IEnumerable<MenuItemDto> FilteredMenuItems =>
        SelectedCategory == null ? MenuItems : MenuItems.Where(m => m.Category == SelectedCategory);

    string ShopDetail = "";
    List<MenuItemDto> MenuItems = new();
    bool showFoodModal = false;
    bool isLoading = true;
    bool isSaving = false;
    string errorMessage = string.Empty;
    string successMessage = string.Empty;
    int editFoodId = 0;
    string newFoodName = string.Empty;
    decimal newFoodPrice = 0;
    FoodCategory newFoodCategory = FoodCategory.MainDish;
    string newFoodImagePath = "/img/food/default.png";

    protected override async Task OnInitializedAsync()
    {
        await LoadShopInfoAsync();
        await LoadMenuItemsAsync();
    }

    void SelectCategory(FoodCategory? cat)
    {
        SelectedCategory = cat;
    }

    void GoBack() 
    { 
        NavigationManager.NavigateTo("/shops"); 
    }

    void GoToOrders()
    {
        NavigationManager.NavigateTo($"/shop-orders/{ShopId}");
    }

    async Task LoadShopInfoAsync()
    {
        try
        {
            var shop = await ApiService.GetShopAsync(ShopId);
            if (shop != null)
            {
                ShopDetail = shop.Name;
            }
        }
        catch 
        { 
            ShopDetail = $"ร้านค้า ID: {ShopId}"; 
        }
    }

    async Task LoadMenuItemsAsync()
    {
        try
        {
            isLoading = true;
            errorMessage = string.Empty;
            MenuItems = await ApiService.GetShopMenuItemsAsync(ShopId);
        }
        catch (Exception ex)
        {
            errorMessage = $"เกิดข้อผิดพลาดในการโหลดเมนู: {ex.Message}";
            MenuItems = new();
        }
        finally
        {
            isLoading = false;
        }
    }

    string GetCategoryText(FoodCategory category)
    {
        return category switch
        {
            FoodCategory.MainDish => "อาหาร",
            FoodCategory.Dessert => "ของหวาน",
            FoodCategory.Drink => "เครื่องดื่ม",
            FoodCategory.Other => "อื่นๆ",
            _ => "ไม่ระบุ"
        };
    }

    void ShowAddFoodModal() 
    {
        editFoodId = 0;
        newFoodName = string.Empty;
        newFoodPrice = 0;
        newFoodCategory = FoodCategory.MainDish;
        newFoodImagePath = "/img/food/default.png";
        showFoodModal = true;
    }

    void ShowEditFoodModal(MenuItemDto item) 
    {
        editFoodId = item.Id;
        newFoodName = item.Name;
        newFoodPrice = item.Price;
        newFoodCategory = item.Category;
        newFoodImagePath = item.ImagePath ?? "/img/food/default.png";
        showFoodModal = true;
    }

    void HideFoodModal() 
    { 
        showFoodModal = false; 
    }

    async Task SaveFoodAsync()
    {
        try
        {
            isSaving = true;
            errorMessage = string.Empty;
            successMessage = string.Empty;

            // Validation
            if (string.IsNullOrWhiteSpace(newFoodName))
            {
                errorMessage = "กรุณากรอกชื่อเมนู";
                return;
            }

            if (newFoodPrice < 0)
            {
                errorMessage = "ราคาต้องมากกว่าหรือเท่ากับ 0";
                return;
            }

            var menuItemData = new CreateMenuItemDto
            {
                Name = newFoodName.Trim(),
                Price = newFoodPrice,
                ShopId = ShopId,
                Category = newFoodCategory,
                ImagePath = string.IsNullOrWhiteSpace(newFoodImagePath) ? "/img/food/default.png" : newFoodImagePath.Trim()
            };

            MenuItemDto? result;
            if (editFoodId == 0)
            {
                // Create new menu item
                result = await ApiService.CreateMenuItemAsync(menuItemData);
                successMessage = "เพิ่มเมนูสำเร็จ!";
            }
            else
            {
                // Update existing menu item
                result = await ApiService.UpdateMenuItemAsync(editFoodId, menuItemData);
                successMessage = "อัปเดตเมนูสำเร็จ!";
            }

            if (result != null)
            {
                await LoadMenuItemsAsync();
                HideFoodModal();
                
                // Clear success message after 3 seconds
                _ = Task.Delay(3000).ContinueWith(_ => 
                {
                    InvokeAsync(() => 
                    {
                        successMessage = string.Empty;
                        StateHasChanged();
                    });
                });
            }
            else
            {
                errorMessage = "เกิดข้อผิดพลาดในการบันทึกข้อมูล";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"เกิดข้อผิดพลาด: {ex.Message}";
        }
        finally
        {
            isSaving = false;
        }
    }

    async Task DeleteFoodAsync(int id)
    {
        if (!await JS.InvokeAsync<bool>("confirm", "ยืนยันการลบเมนูนี้?")) return;
        
        try
        {
            errorMessage = string.Empty;
            successMessage = string.Empty;

            var success = await ApiService.DeleteMenuItemAsync(id);
            if (success)
            {
                await LoadMenuItemsAsync();
                successMessage = "ลบเมนูสำเร็จ!";
                
                // Clear success message after 3 seconds
                _ = Task.Delay(3000).ContinueWith(_ => 
                {
                    InvokeAsync(() => 
                    {
                        successMessage = string.Empty;
                        StateHasChanged();
                    });
                });
            }
            else
            {
                errorMessage = "ไม่สามารถลบเมนูได้";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"เกิดข้อผิดพลาดในการลบ: {ex.Message}";
        }
    }

    // Table Management
    bool showTableModal = false;
    List<TableDto> tables = new();
    bool isAddingTable = false;
    string newTableName = "";
    int nextTableNumber = 1;
    bool isLoadingTables = false;
    string tableErrorMessage = "";
    string tableSuccessMessage = "";

    void ShowTableManagementModal()
    {
        showTableModal = true;
        _ = LoadTablesAsync();
    }

    void HideTableModal()
    {
        showTableModal = false;
        newTableName = "";
        tableErrorMessage = "";
        tableSuccessMessage = "";
    }

    async Task LoadTablesAsync()
    {
        try
        {
            isLoadingTables = true;
            tables = await ApiService.GetTablesByShopAsync(ShopId);
            
            // Calculate next table number
            nextTableNumber = tables.Count + 1;
            newTableName = $"โต๊ะ {nextTableNumber}";
        }
        catch (Exception ex)
        {
            tableErrorMessage = $"เกิดข้อผิดพลาดในการโหลดโต๊ะ: {ex.Message}";
        }
        finally
        {
            isLoadingTables = false;
            StateHasChanged();
        }
    }

    async Task AddTableAsync()
    {
        if (string.IsNullOrWhiteSpace(newTableName)) return;

        try
        {
            isAddingTable = true;
            tableErrorMessage = "";
            tableSuccessMessage = "";

            var createTableDto = new CreateTableDto
            {
                Name = newTableName.Trim(),
                ShopId = ShopId
            };

            var result = await ApiService.CreateTableAsync(createTableDto);
            if (result != null)
            {
                await LoadTablesAsync();
                tableSuccessMessage = "เพิ่มโต๊ะสำเร็จ!";
                
                // Auto-generate next table name
                nextTableNumber = tables.Count + 1;
                newTableName = $"โต๊ะ {nextTableNumber}";

                // Clear success message after 3 seconds
                _ = Task.Delay(3000).ContinueWith(_ => 
                {
                    InvokeAsync(() => 
                    {
                        tableSuccessMessage = string.Empty;
                        StateHasChanged();
                    });
                });
            }
            else
            {
                tableErrorMessage = "ไม่สามารถเพิ่มโต๊ะได้";
            }
        }
        catch (Exception ex)
        {
            tableErrorMessage = $"เกิดข้อผิดพลาดในการเพิ่มโต๊ะ: {ex.Message}";
        }
        finally
        {
            isAddingTable = false;
        }
    }

    async Task DeleteTableAsync(int tableId)
    {
        if (!await JS.InvokeAsync<bool>("confirm", "ยืนยันการลบโต๊ะนี้?")) return;
        
        try
        {
            tableErrorMessage = "";
            tableSuccessMessage = "";

            var success = await ApiService.DeleteTableAsync(tableId);
            if (success)
            {
                await LoadTablesAsync();
                tableSuccessMessage = "ลบโต๊ะสำเร็จ!";
                
                // Clear success message after 3 seconds
                _ = Task.Delay(3000).ContinueWith(_ => 
                {
                    InvokeAsync(() => 
                    {
                        tableSuccessMessage = string.Empty;
                        StateHasChanged();
                    });
                });
            }
            else
            {
                tableErrorMessage = "ไม่สามารถลบโต๊ะได้";
            }
        }
        catch (Exception ex)
        {
            tableErrorMessage = $"เกิดข้อผิดพลาดในการลบโต๊ะ: {ex.Message}";
        }
    }
}
